#ifndef SOLVER_H_
#define SOLVER_H_

#include <vector>
#include <utility>
#include "Generator.h"
using namespace std;

typedef pair<int, int> pii;
void solver(int w[maxn][maxn], int n, int m, vector <pii> &ans); 

#endif
